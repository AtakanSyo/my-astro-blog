---
layout: ../../layouts/simLayout.astro
title: "Angular Size & Physical Size Calculator"
category: simulation
writer: Astrosyo
pubDate: '2026-08-29'
description: "Give any two of angular size, physical size, and distance, and get the third using the exact trigonometric relation. The small-angle approximation shown alongside for comparison."
postColor: rgba(74, 44, 20, 1)
slug: angular-size-calculator
keywords:
  - angular size calculator
  - angular diameter calculator
  - small angle approximation
  - apparent size astronomy
  - angular size formula
  - physical size from angular size
  - astronomy angle calculator
lang: en
---

import SimPostSchema from "../../components/SimPostSchema.astro";
import Changelog from "../../components/Changelog.astro";
import ViewSource from "../../components/ViewSource.astro";
import AngularSizeCalculator from "../../components/simulations/angular-size-calculator/AngularSizeCalculator.jsx";

<figure class="post-figure post-figure-md" id="calculator">
  <AngularSizeCalculator client:load />
</figure>

The Moon and the Sun look almost exactly the same size in Earth's sky.
Not because they're actually similar in size (the Sun is about 400 times
wider), but because the Sun also happens to be about 400 times farther
away, which by the way, is 
a very interesting coincidence. 

That's the whole idea behind angular size: how big something *looks*
depends on both its true size and its distance, and you can't tell which
from the angle alone.

## The geometry

Picture a flat object of true diameter D, centered a distance d away and
facing the observer broadside-on — its diameter perpendicular to the line
of sight, like a disc or a ruler held up, not a ball the observer could
stand inside of. The angle it subtends, θ, is the apex angle of the
triangle formed by the observer and the two ends of the object. The
triangle splits into two right triangles with the right angle at the
object's own center, adjacent side d, and opposite side D/2:

$$
\tan\!\left(\frac{\theta}{2}\right) = \frac{D/2}{d} \qquad\Longrightarrow\qquad \theta = 2\arctan\!\left(\frac{D}{2d}\right)
$$

This is exact for any angle from 0° up to (but not including) 180°, for
any physical size relative to the distance — a wider, closer object just
pushes θ toward 180° without ever reaching it.

## The small-angle approximation, and when it holds

For small θ, tan(θ/2) ≈ θ/2 (also holds for tan(θ) ≈ θ, but naturally the 
approximation threshold for θ doubles), and the exact relation collapses to:

$$
\theta \approx \frac{D}{d}
$$

The calculator always computes the exact value, but shows the
small-angle result and the percent difference between the approximation
and true-size side by side,
so you can see directly where the approximation is "close-enough" and
where it wouldn't be.

## Angular size vs. physical size

These are easy to conflate but describe different things. **Physical
size** is an object's actual linear diameter: a number in meters or
light-years that doesn't depend on who's looking or from where.
**Angular size** is how large it *appears*, and depends equally on
physical size and distance. The same galaxy would look larger if it were
closer, without a change in its true size at all. A measured angular size, on its own, is
not a measurement of true size; you need the distance too.

<SimPostSchema
  title="Angular Size & Physical Size Calculator"
  description="Give any two of angular size, physical size, and distance, and get the third — using the exact trigonometric relation, with the small-angle approximation shown alongside for comparison."
  url="https://astrosyo.com/posts/angular-size-calculator"
  datePublished="2026-08-29"
  authorName="Astrosyo"
  keywords={["angular size calculator", "angular diameter calculator", "small angle approximation", "apparent size astronomy", "physical size from angular size"]}
  articleSection="Astronomy Tools"
  isAccessibleForFree={true}
  publisherName="Astrosyo"
  publisherLogo="/images/shared/logo.svg"
  hasPart={{
    "@type": "SoftwareApplication",
    "name": "Angular Size and Physical Size Calculator",
    "applicationCategory": "EducationalApplication",
    "operatingSystem": "Any",
    "browserRequirements": "Requires modern browser",
    "url": "https://astrosyo.com/posts/angular-size-calculator#calculator",
    "description": "Interactive calculator that solves for angular size, physical size, or distance given the other two, using the exact trigonometric angular-size relation with the small-angle approximation shown for comparison."
  }}
/>

<ViewSource path="src/components/simulations/angular-size-calculator/AngularSizeCalculator.jsx" />

<Changelog
  entries={[
    "2026-08-31: Switched the exact formula from arcsin (models the object as a solid sphere, capping physical size at twice the distance) to arctan (models a flat object facing the observer, no such cap).",
  ]}
/>
