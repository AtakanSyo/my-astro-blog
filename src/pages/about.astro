---
import Layout from '../layouts/Layout.astro';
---

<Layout title="About Astrosyo">
  <main class="about-page">
    <section class="intro">
      <h1>Welcome to Astrosyo</h1>
      <p>
        Hey 👋 I’m really glad you’re here. Astrosyo exists because looking up at the night sky
        changes something in us — and I wanted a place to share that feeling. If you’re curious
        about telescopes, space, or just want to start stargazing without getting overwhelmed,
        you’re in the right spot.
      </p>
    </section>

    <section class="mission">
      <h2>Our Mission</h2>
      <p>
Everything I write or build here — reviews, guides, tools, and simulations — 
comes from real experience and genuine curiosity. I started this blog to offer something useful in a time
 when most sites are packed with ads and distractions. My goal is simple: keep things honest, fast, easy to understand, 
 and pleasing to look at.
      </p>
    </section>

  <section class="coverage">
    <h2>What You’ll Find Here</h2>
    <ul>
      <li>
        <strong>Telescope Reviews</strong> — some are based on real, hands-on experience and others are research-based.
        Each review is labeled clearly ("hands-on" or "research") so you always know what you're reading.
      </li>
      <li>
        <strong>Getting Started</strong> — simple guides to help you understand your telescope and enjoy your first nights under the stars.
      </li>
      <li>
        <strong>Astronomy Simulations</strong> — visual demos and interactive experiments to help explain how things in space actually work.
      </li>
      <li>
        <strong>Astronomy Tools</strong> — calculators and helpers (FOV, magnification, exit pupil, etc.) to make observing easier.
      </li>
      <li>
        <strong>Space & Curiosity Posts</strong> — topics about NASA, discoveries, and anything fascinating happening above our heads.
      </li>
    </ul>
  </section>

    <section class="team">
      <h2>Who’s Behind Astrosyo?</h2>
      <p>
        It’s mostly just me — someone who genuinely loves space, spends too much time testing gear,
        and wants to help people avoid buying the wrong telescope. Sometimes I collaborate with
        friends who have backgrounds in astronomy, astrophotography, or engineering 🚀
      </p>
    </section>

    <section class="join">
      <h2>Stay in the Loop</h2>
      <p>
        If you’d like, you can join the mailing list — no spam, just a quick email when there’s
        something worth sharing: new guides, telescope deals, or cool space events.
      </p>
      <a href="/newsletter" class="btn-primary">Subscribe</a>
    </section>
  </main>
</Layout>



<style>
/* Page wrapper */
.about-page {
  max-width: 980px;
  margin: 0 auto;
  padding: calc(var(--spacing) * 1.75) var(--spacing) calc(var(--spacing) * 2.25);
  color: var(--text-color);
}

/* Section spacing */
.about-page section { margin-bottom: calc(var(--spacing) * 1.75); }

/* Intro */
.about-page .intro {
  text-align: center;
  margin-bottom: calc(var(--spacing) * 2);
}
.intro h1 {
  font-size: clamp(1.9rem, 3.1vw, 2.6rem);
  margin: 0 0 .5rem;
  font-weight: 800;
  letter-spacing: .2px;
}
.intro p {
  font-size: clamp(1rem, .98rem + .25vw, 1.125rem);
  line-height: 1.7;
  color: var(--meta-color);
  margin: 0 auto;
  max-width: 70ch;
}

/* Top ribbon accent */
.about-page .mission::before,
.about-page .coverage::before,
.about-page .team::before,
.about-page .join::before {
  content: "";
  position: absolute; left: 0; right: 0; top: 0; height: 3px;
  background: var(--cosmic-gradient);
  opacity: .95;
}

/* Section headings */
.about-page .mission h2,
.about-page .coverage h2,
.about-page .team h2,
.about-page .join h2 {
  margin: 0 0 .4rem;
  font-size: clamp(1.25rem, 1.05rem + .65vw, 1.85rem);
  font-weight: 800;
  letter-spacing: .2px;
}

/* Body copy */
.about-page .mission p,
.about-page .team p {
  margin: 0;
  color: var(--meta-color);
  font-size: .98rem;
  line-height: 1.7;
}

/* Coverage list */
.coverage ul {
  list-style: none;
  padding: 0;
  margin: .25rem 0 0;
  display: grid;
  gap: .4rem .75rem;
  grid-template-columns: 1fr;
}
.coverage li {
  color: var(--meta-color);
  font-size: .98rem;
  line-height: 1.65;
  position: relative;
  padding-left: 1.15rem;
}
.coverage li::before {
  content: "";
  position: absolute;
  left: 0; top: .65em;
  width: .46rem; height: .46rem;
  border-radius: 999px;
  background: var(--cosmic-gradient);
  box-shadow: 0 0 0 2px rgba(255,0,60,.2);
}
@media (min-width: 720px) {
  .coverage ul { grid-template-columns: 1fr 1fr; }
}

/* CTA BUTTON — the only hover-animated element */
.btn-primary {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  padding: .78rem 1.15rem;
  border-radius: 999px;
  text-decoration: none;
  font-weight: 750;
  letter-spacing: .2px;
  color: #ffffff;
  background: var(--cosmic-gradient);
  border: 1px solid var(--brand-red-darker);
  box-shadow: 0 8px 22px rgba(255,0,60,.28);
  transition: transform .18s ease, box-shadow .18s ease, filter .18s ease;
}
.btn-primary::after {
  content: "→";
  opacity: .9;
  transition: transform .18s ease;
}
.btn-primary:hover {
  transform: translateY(-1px);
  filter: brightness(1.1);
  box-shadow: 0 12px 28px rgba(255,0,60,.36);
}
.btn-primary:hover::after { transform: translateX(3px); }
.btn-primary:active {
  transform: translateY(0);
  box-shadow: 0 6px 16px rgba(255,0,60,.22);
}
.btn-primary:focus-visible {
  outline: none;
  box-shadow:
    0 0 0 3px rgba(255,255,255,.4),
    0 0 0 6px var(--brand-red-bg);
}

/* Button placement */
.about-page .join .btn-primary { margin-top: .6rem; }

/* Content rhythm */
.about-page :where(p, ul, ol) + :where(p, ul, ol) { margin-top: .55rem; }

/* Mobile polish */
@media (max-width: 640px) {
  .about-page { padding: calc(var(--spacing) * 1.4) var(--spacing) calc(var(--spacing) * 2); }
}
</style>